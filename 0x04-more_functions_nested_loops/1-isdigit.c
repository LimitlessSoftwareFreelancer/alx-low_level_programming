#include "holberton.h"

/**
 * _isupper checks for uppercase character
 * declared in header
 * Return: 1 if @c is uppecase
 */
int _isdigit(int c)
{
	if (c > 48 && c <= 57)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
